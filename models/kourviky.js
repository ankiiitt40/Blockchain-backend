// Arithmetic
let sum = 5 + 3;
let diff = 5 - 3;

// Comparison
5 == "5";   // true (value only)
5 === "5";  // false (value + type)

// Logical
true && false; // AND
true || false; // OR
!true;         // NOT
